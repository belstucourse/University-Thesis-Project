spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://localhost:8003
      name: auth-service
      fail-fast: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/